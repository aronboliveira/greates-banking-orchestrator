apiVersion: v1
kind: Service
metadata:
  name: pismo-api-service
  namespace: pismo-api
  labels:
    app: pismo-api
spec:
  type: ClusterIP
  selector:
    app: pismo-api
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
