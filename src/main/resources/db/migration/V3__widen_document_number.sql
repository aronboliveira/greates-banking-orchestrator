-- Already widened in V1 (VARCHAR(512)) for KMS ciphertext. This migration is a
-- no-op but kept as a placeholder so the V-numbering matches the handoff
-- documentation references. Remove this file if you delete the reference in
-- handoff.md.
SELECT 1;
